---
description: 0–100 可调百分比参数统一使用水平滑动条，禁止用 SpinBox 代替
alwaysApply: true
---

# 百分比调节：统一用滑动条

凡用户可连续调节、取值范围为 **0–100**（或不透明度/强度/流量等等价百分比）的控件，**必须使用 `QSlider`（水平）**，不要用 `QSpinBox` / 下拉百分比框作为主调节方式。

## 要求

1. **布局**：`标签` + `QSlider(0…100)` + `右侧数值标签`（如 `100%`）；滑动时同步更新右侧文案  
2. **已有控件**：不透明度、填充，以及后续画笔不透明度/流量/硬度、通道不透明度等，一律按此模式  
3. **同步改 `.ui`**：在 Designer/`*.ui` 中放 `QSlider`，逻辑里 `valueChanged(int)` 接线；禁止只在 `.cpp` 里临时 `new QSlider` 绕过表单  
4. **禁用态**：无文档/无活动项时，滑条与数值标签一并 `setEnabled(false)`

## 反例

- 用不透明度 `QSpinBox`、填充 `QSpinBox` 代替滑条  
- 0–100 参数做成可编辑下拉框却无滑条主控

## 例外

- 需要精确键入整数且**不是**「拖动调节」场景（如画布宽高像素）——可用 SpinBox；百分比类强度调节仍用滑条
