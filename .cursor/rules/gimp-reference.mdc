---
description: 纯 UI 对齐 PS；功能阶段须先对照 gimp-master 代码组织
alwaysApply: true
---

# 参考源分流：PS 布局 vs GIMP 组织

本仓库分两阶段参考，**不要混用**：

| 阶段 | 主要参考 | 不做什么 |
|------|----------|----------|
| **纯 UI**（壳、布局、面板外观、工具条、图标位、标题栏等） | **Photoshop** 布局与观感（截图 / 真机 / 用户指定） | 不为「长得像」去抄 GIMP 的 GTK 控件树 |
| **功能编写**（domain / engine / tools / 面板接线 / 文档模型） | 先查 **`gimp-master` 的代码组织与职责边界**，再做本项目瘦身版 | 禁止只凭 PS 使用习惯或聊天脑补架构落地 |

## 纯 UI 阶段

- 对齐 PS：分区、Tab 顺序、控件疏密、暗色工作区习惯等。
- `.ui` 优先；样式走 QSS。注释可写「对齐 PS xxx」，**不必**强行找 GIMP 对等控件。
- 若 UI 改动会牵出行为（点击后改文档/合成），则已跨入功能阶段，须按下条对照 GIMP。

## 功能编写阶段（强制对照 GIMP）

写/改 **功能代码**前：

1. 在 `gimp-master` 定位对应模块（如 layers dock、paint、selection、item tree）
2. 读清：**谁持有真相、信号/命令怎么走、UI 与 core 如何分层**
3. 按本项目规模做瘦身实现；**参考组织思路，不搬源码**
4. 在注释或 `docs/` 写明：参考了哪些路径/概念、本项目简化掉了什么

**禁止**：用户未明确要求时擅自新开功能；禁止未查 `gimp-master` 就落地 domain/engine/tools 或面板业务接线。

## 当前已知债

- 右侧面板：**结构已对照** GIMP `ItemTreeView` → `Layer/Channel/PathTreeView`（见 `ui/itemtreepanel.*` 等）；外观用 PS 三 Tab 壳（`LayerPanel`）
- 通道 / 路径：仅 UI 骨架，无 domain；底栏尚未抽成独立 actions/commands 层
- 图层：已接线文档增删/显隐/透明度；Mode/锁/蒙版/组等仍为占位

## 与注释

凡新写代码必须按 `code-comments.mdc` 写中文注释。  
- UI 壳：可标「对齐 PS」  
- 功能：须标 GIMP 对照结论，或明确写「待对照」且不得假装已对照  
