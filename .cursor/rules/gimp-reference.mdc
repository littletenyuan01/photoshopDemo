---
description: 实现功能须对照 gimp-master；禁止凭印象擅自扩功能
alwaysApply: true
---

# 对照 GIMP 实现（强制）

## 要求

1. **写/改功能代码前**：先在 `gimp-master` 中定位对应模块（如 layers dock、paint、selection），阅读职责边界后再设计本项目的瘦身版  
2. **注释或 docs 中写明**：参考了 GIMP 的哪些路径/概念；本项目简化掉了什么  
3. **禁止**：用户未明确要求时擅自新开功能；禁止只凭 Photoshop 使用习惯或聊天里的架构脑补落地  

## 当前已知债

- 右侧面板：**结构已对照** GIMP `ItemTreeView` → `Layer/Channel/PathTreeView`（见 `ui/itemtreepanel.*` 等）；外观用 PS 三 Tab 壳（`LayerPanel`）
- 通道 / 路径：仅 UI 骨架，无 domain；底栏尚未抽成独立 actions/commands 层
- 图层：已接线文档增删/显隐/透明度；Mode/锁/蒙版/组等仍为占位

## 与注释

凡新写代码必须按 `code-comments.mdc` 写中文注释；对照 GIMP 的结论也要写进注释或 `docs/`。
