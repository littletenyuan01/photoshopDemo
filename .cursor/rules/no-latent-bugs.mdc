---
description: 交付纪律——禁止留下静默失败的隐患；每条验证要求与真实踩坑对应
alwaysApply: true
---

# 禁止留下有隐患的代码

**核心判据**：凡是「**不报错、不崩溃、不警告，但就是不工作**」的路径，一律视为缺陷。
本文件每条规则都对应本项目**真实踩过的坑**（附位置），不是通用教条。

---

## 1. 不变量交给编译器，不交给"记得"

需要"**所有路径都必须做 X**"时（如入栈必挂 owner、改状态必 push）：

1. 把入口收成**一个**函数；
2. 把其他路径设为 `private` / `friend`，让绕过**编译不过**；
3. 写一个"绕过写法"编译一次，确认真的报错。

> **反例（真实）**：`Layer::owner` 只有 `addTransparentLayer` 挂了；`createBlank` 与
> 「打开图片」流程漏挂 → 改背景层**显隐/透明度/改名全部静默不生效**（实测 `contentChanged` 发 0 次）。
> 修法：`LayerStack` 改栈方法改 `private` + `friend ImageDocument`，`ImageDocument::addLayer()` 成唯一入口。

## 2. 把「显式」换成「隐式」时，必须列全入口点

隐式机制（信号 / 回调 / RAII / 自动注册）的特征是**漏了不报错**。替换时必须：

1. 先 `grep` 出**所有**创建点与入口点，写成清单；
2. 逐条改并勾掉；
3. **为每个入口写一条行为验证**。

> **反例（真实）**：把「UI 手动 `notifyLayerVisualChanged()`」改成「owner 自动广播」，
> 只改了正在编辑的那个函数，没回头审计其它创建路径。

## 3. 状态变更路径必须做**行为验证**，`grep` 不算验证

- `grep` 只能证明「没有绕过」，**不能证明「真的生效」**。
- 涉及信号 / 回调 / 跨层通知的改动，必须写一次性验证程序并**断言结果**
  （信号发了几次、值变没变、画布有没有刷新）。
- 结论必须是**可复现的数字或输出**，不能是"看起来对"。

> **反例（真实）**：只验证了「`ui/*.cpp` 里 `layer->set*` 出现 0 处」就宣布收口成功，
> 没验证「改背景层显隐会不会发 `contentChanged`」——一测就是 **0 次**。

## 4. 热路径代码：判断前先量

出现在「每次鼠标移动 / 每次重绘 / 每次信号」路径上的代码，**必须先微基准量过再下结论**。

**禁止**在注释或文档里写「代价可接受」「影响不大」这类**没有数据**的判断。

> **反例（真实）**：通道面板注释写「通道数固定 4 行、代价可接受」，
> 实测 4000×3000 下 **216 ms/次**（画布自身才 60 ms）→ 约 3.6 FPS，基本没法画。

## 5. 列表刷新：警惕 `clear()` + 重建打回用户状态

任何 `clear()` + 重建，都要问：**选中项 / 正在编辑的项 / 滚动位置会不会被重置？**

像素级变化只应**改受影响行的内容**，不应重建整表。

> **反例（真实）**：`ChannelTreePanel` 曾把 `contentChanged` 直连重建 →
> **每次画笔移动，通道选中项被打回第 0 行**。

## 6. 位图 / 缩放相关必须验证 `devicePixelRatio`

- 任何「生成位图塞进 `QIcon` / `QPixmap`」的代码都要考虑 DPR：
  要么产出**多档 DPR**，要么让 Qt **从足够大的源图**缩。
- **预览 / 验证工具必须复刻真实渲染路径**，否则「预览正常、真机翻车」。

> **反例（真实，踩过两次）**：
> ① SVG `render(painter)` + 手动设 DPR → 双重缩放，**只显示左上角一角**；
> ② 工具箱先把源图缩到 20×20 再包进单张 DPR=1 的 `QIcon` → 屏幕缩放时被放大**发虚**。

## 7. 含非 ASCII 的文件改写，一律用编辑工具

**禁止**用 `PowerShell` / shell 读写往返含中文的源码或文档。以下写法**全部禁止**：

- `(Get-Content f) | Set-Content f` / `Set-Content` 截断重写
- `Add-Content` 追加
- `-replace` / `.Replace(...)` 后回写
- 用 `Select-String` 求"第几行"再切片重写

这些写法会**同时**踩两个坑：**编码被转成 GBK 乱码**，以及**切片下标算错导致文件被截断**
（两者都不可逆，且不一定报错）。

> **反例（真实，踩过三次）**：① `.Replace(...) | Set-Content` 把 C++ 与 `.ui` 里的中文变乱码；
> ② 又一次同样操作；
> ③ 用 `Get-Content`+`Select-String`+`Set-Content` 从 `dark.qss` 里"删掉临时探针块"，
> 结果整份 210 行样式表被截成 8 行、首行还变成乱码，只能 `git checkout` 重来。

**若已写坏**：立刻 `git checkout -- <file>` 从版本库恢复，然后**用编辑工具重做**；
**不要**在乱码上继续改。**要想清楚**：这类文件一律用 `edit` / `write` 工具改。

## 8. 文档与实际必须一致

改完代码回头核对 `docs/`：**已写的能力是否真的存在**。宁可写"尚未实现"，也不要过度声明。

> **反例（真实）**：`features.md` 声称图层面板支持「上移/下移」，
> 实际既无按钮也无菜单项，`LayerStack::moveLayer` 是死代码。

## 9. 样式 / 资源绑定「不生效也不报错」，必须实测

`QSS` 选择器、资源别名、`.ui` 里的提升控件名，都属于**绑不上也不会报错**的类型：

1. 写完样式后，**实测目标控件真的拿到了颜色/图片**（取实际像素或断言 `icon().isNull()`），
   不能只看「代码里有这行规则」。
2. 选择器**优先用类名**（`ColorsPanel QToolButton`），不要用提升实例名
   （`QWidget#colorsPanel …`）做祖先：实例名是 `setupUi` 阶段才设上去的，实测匹配不上。
3. 中文键值（占位数据表、按名字取图标）**必须 `QString::fromUtf8`**，
   禁止 `QLatin1String("中文")` —— 它按 Latin-1 解码 UTF-8 字节，**永远比不中**。
4. 全局样式（如 `QWidget { background: transparent }`、`QToolButton:checked`）会**兜到你没写的地方**：
   面板/页面这类容器要**显式给底色**，否则深色窗口下露出黑底。

> **反例（真实）**：本轮新增两个面板时，`QWidget#colorsPanel …` 一整段规则**一条都没命中**——
> 面板底是黑、分区标题变成全局 `:checked` 蓝按钮；同时 `QLatin1String("纯红")` 让
> **12 个色块、6 个渐变缩略图、6 个图案花纹全部静默消失**，构建 0 警告、断言「12 行都在」也照样通过。
> 教训：**断言「状态在」不等于「东西画出来了」**，要断言绘制结果。

## 10. 交付前自查（每条都要能给出证据）

| 检查项 | 证据形式 |
|---|---|
| 全量重建 | **0 error / 0 warning** |
| 不变量是否编译器保证 | 写一个"绕过写法"，确认**编译不过** |
| 状态变更是否生效 | 一次性验证程序 + **断言输出** |
| 热路径开销 | **微基准的前后数字** |
| UI 状态是否被打回 | 验证选中项 / 编辑态 / 滚动位置 |
| **样式/资源/图标是否真的画出来** | **屏幕实拍取色 / 断言 `icon()` 非空且互不相同** |
| 文档是否与实现一致 | 逐条对照 `docs/` 与实际 UI / 菜单 |
| 临时验证文件 | **用完即删**，不入库 |

---

## 禁止

- 用 `grep` / 静态检查的通过，替代**行为验证**
- 把"所有路径都要做的事"散在多个调用点，靠人记得
- 在注释/文档里写无数据的性能判断
- 改了机制却只审计正在编辑的那一个入口点
- 在 `docs/` 里声明未实现的能力（过度声明）
- 用 shell 读写往返含中文的文件
- 验证工具不复刻真实路径（导致"预览正常、真机翻车"）
- 只断言「状态/数据在」，不断言「界面真的画出来了」
