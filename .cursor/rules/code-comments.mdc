---
description: 关键代码必须写清晰注释，便于阅读与面试讲解
alwaysApply: true
---

# 代码注释约定

## 必须注释的地方

1. **每个头文件中的类**：用简短块注释说明职责、对应架构层（ui/domain/engine…）、关键不变量  
2. **非显然的成员函数 / 算法**：说明输入输出、坐标空间（图像坐标 vs 控件坐标）、线程假设  
3. **像素格式与混合 / 合成 / 选区 / 蒙版**：写明预乘还是直通、扫描顺序、与谁相交  
4. **参考源**：纯 UI 可注「对齐 PS xxx」；功能代码须注 GIMP 路径/概念与取舍，未对照时标明「待对照」（见 `gimp-reference.mdc`）  
5. **`.ui` 提升控件、信号槽接线处**：说明谁拥有数据、谁只负责显示  
6. **与 OpenCV/CUDA 相关的取舍**：一两句说明为什么这样写

## 范围

- **凡本次新写或改动的 `.h/.cpp` 关键逻辑，都必须带齐注释后再结束任务**  
- 不能只改行为不补注释  

## 怎么写

- 用**中文**（与项目文档一致）；符号名保持英文  
- 写「为什么 / 约定」，少写「这是一个赋值」式废话  
- 复杂函数：函数头注释 + 关键步骤行内注释；不要每行都注  

## 反例

- 无注释的大段混合循环、坐标变换、撤销瓦片逻辑  
- 只改代码不同步注释，导致注释与实现相反  

新增或修改关键逻辑时，**同步补注释**；纯重命名可只更新被改处注释。
