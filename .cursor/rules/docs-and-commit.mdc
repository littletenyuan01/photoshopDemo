---
description: 代码变更必须同步更新 docs，并在完成后给出 commit 文案
alwaysApply: true
---

# 文档同步与 Commit 文案

## 文档目录

技术文档一律写在仓库根目录 `docs/`（不是 `wiki/`）。

- `wiki/`：项目定位、路线、构建入口（偏规划）
- `docs/`：功能说明、结构、重点代码、技术点（偏实现，**随代码更新**）

## 何时更新 docs

以下任一发生时，必须在同一次任务里更新对应文档，禁止只改代码不改文档：

- 新增 / 修改 / 删除功能
- 调整模块结构或目录
- 引入重要技术点、算法、数据结构
- 关键代码路径或公共 API 变化

## docs 应记录的内容

按主题维护 Markdown，至少覆盖：

1. **功能介绍**：做什么、怎么用、当前限制
2. **结构**：目录/模块职责、数据流
3. **重点代码**：关键类/函数与文件路径（可用短摘录 + 说明）
4. **技术点**：选型理由、难点、与 GIMP 参考的对应关系（若有）

小改动可只改相关章节；新功能应新增或扩展对应文档页，并更新 `docs/README.md` 索引。

## 每次开发完成后的输出

完成一个小功能或一轮代码更新后，在回复末尾给出**可直接使用的 commit 文案**（中文或中英均可），格式：

```text
<一句话概括为什么改>

可选：1–3 条补充要点
```

要求：

- 侧重「为什么」，准确对应本次改动
- 只给文案，**不要擅自 `git commit`**，除非用户明确要求提交
- 若本次还改了 `docs/`，文案中应体现文档同步
- 给文案**之前**必须先过一遍 `no-latent-bugs.mdc` 的 §9 交付前自查表；
  哪一项没做，就在回复里**明说没做**，不要含糊过去

## 反例

- 改完画笔逻辑却不更新 `docs/` 中绘制相关说明
- 只说「已完成」却不给 commit 文案
- 把实现细节只写在聊天里、不落盘到 `docs/`
